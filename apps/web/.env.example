# API base URL (e.g. http://localhost:3000 when running Rails in dev)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Google OAuth (used for "Sign in with Google" button redirect)
# The actual OAuth is handled by the API; the frontend just redirects to it
